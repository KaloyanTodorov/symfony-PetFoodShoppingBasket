{% extends 'base.html.twig' %}

{% block body %}
    <div class="form-group col-md-4 col-md-offset-4">
        <h3>Add Product:</h3>
        {{ form_start(productForm) }}
        {{ form_widget(productForm) }}


        {{ form_end(productForm) }}

        <button type="submit" class="btn-primary btn btn-1">Add product</button><br>
        <a href="{{ path('all_products') }}" class="btn-success btn btn-1" >Back</a>
    </div>
{% endblock %}