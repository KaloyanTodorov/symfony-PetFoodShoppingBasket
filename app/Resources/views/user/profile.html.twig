{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h2> Hello, {{ app.user.username }}</h2>

        <div class="col-md-4">
            Currently you have {{ app.user.initialCash }} lv.<br/>
            <a href="{{ path('homepage') }}" class="btn-default btn btn-sm">Home</a>
            <a href="{{ path('edit_user_profile_form', {'id': app.user.id}) }}" class="btn-primary btn btn-sm">Edit profile</a>
            <a href="{{ path('logout') }}" class="btn-danger btn btn-sm">Logout</a>
             {#<a href="{{ path('cart_show') }}" class="btn-warning btn btn-sm">Show cart</a>#}
        </div>
    </div>
{% endblock %}